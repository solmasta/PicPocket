# Backend

This backend exposes a minimal Node.js HTTP server for the PicPals Horses app.

## Routes

- `GET /` - returns a status message
- `GET /api/health` - returns a simple health payload

## Scripts

- `npm start` - start the server
- `npm test` - placeholder test command
